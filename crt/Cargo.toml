[package]
name = "crtty-crt"
version = "0.1.2"
edition = "2021"
description = "CRT monitor effect for kitty — powered by crtty"
license = "MIT"
repository = "https://github.com/kosa/CRTty"

[lib]
name = "crtty_crt"
crate-type = ["cdylib"]

[dependencies]
crtty = { path = ".." }
